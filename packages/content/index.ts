// @sinnapi/content — shared, framework-agnostic content (no React / no MUI),
// so it is safe to import anywhere: server components, build scripts, and the
// Next.js (web-public) + Vite (client/vendor portals) apps alike.
export * from './legal';
