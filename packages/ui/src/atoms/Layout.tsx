'use client';
// Layout primitives — pure re-exports preserve MUI's polymorphic `component`
// prop typing (e.g. <Box component="form" onSubmit={…}>).
export {
  Box,
  type BoxProps,
  Container,
  type ContainerProps,
  Stack,
  type StackProps,
  Grid,
  type GridProps,
} from '@mui/material';
