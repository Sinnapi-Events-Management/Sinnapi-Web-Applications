'use client';
/**
 * Convenience re-export of the full Material icon set.
 *   import { Search, Delete } from "@sinnapi/ui/icons";
 *
 * For the smallest bundles you can still import individual icons directly
 * (`import Search from "@mui/icons-material/Search"`); both are tree-shaken by
 * Vite, and web-public's next.config `modularizeImports` rewrites these too.
 */
export * from '@mui/icons-material';
