import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Vendor Terms',
  alternates: { canonical: '/vendor-terms' },
};

export { default } from '@/containers/vendorTerms';
