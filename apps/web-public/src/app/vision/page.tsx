import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Vision',
  description: "Sinnapi's vision.",
  alternates: { canonical: '/vision' },
};

export { default } from '@/containers/vision';
