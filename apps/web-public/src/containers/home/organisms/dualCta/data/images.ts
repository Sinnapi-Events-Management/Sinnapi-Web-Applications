import { IMAGES } from '@/lib/assets';

/** Background photo behind the teal "Planning an event?" client card. */
export const CLIENT_CTA_IMAGE = IMAGES.ceremonyLawn;

/** Background photo behind the gold "Grow your business" vendor card. */
export const VENDOR_CTA_IMAGE = IMAGES.cakeLeopard;
