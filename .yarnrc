# A transitive lint-only dep (eslint-visitor-keys@5) declares Node >=20.19, but the
# project runs fine on Node 20.x. Relax the strict engine check so `yarn install`
# works out of the box for everyone. (This affects install-time checks only.)
--install.ignore-engines true
--add.ignore-engines true
